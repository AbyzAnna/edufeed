# Pattern Inconsistencies

*Auto-generated by Intelligent Analyzer*

## ICS-001: Multiple frontend patterns in use

**Priority**: P2

Found 4 different frontend patterns: Next.js App Router with Server Components (Website feed); React Query (TanStack Query) for Data Fetching (Website feed); Tailwind CSS Styling (Website feed); Expo/React Native Mobile App (Website feed)

**Remediation**:
1. Review the conflicting patterns across repositories
2. Create an ADR documenting the standard pattern to use
3. Create migration tickets for non-compliant repos
4. Update linting rules to enforce the standard

## ICS-002: Multiple data patterns in use

**Priority**: P2

Found 4 different data patterns: PostgreSQL with Prisma ORM (Website feed); Vector Database with Vectorize (Website feed); Cloudflare D1 Database (Website feed); Cloudflare KV Cache (Website feed)

**Remediation**:
1. Review the conflicting patterns across repositories
2. Create an ADR documenting the standard pattern to use
3. Create migration tickets for non-compliant repos
4. Update linting rules to enforce the standard

## ICS-003: Multiple auth patterns in use

**Priority**: P2

Found 3 different auth patterns: Supabase Authentication (SSR) (Website feed); NextAuth.js with JWT Sessions (Website feed); Next.js Middleware for Auth (Website feed)

**Remediation**:
1. Review the conflicting patterns across repositories
2. Create an ADR documenting the standard pattern to use
3. Create migration tickets for non-compliant repos
4. Update linting rules to enforce the standard

## ICS-004: Multiple cloud patterns in use

**Priority**: P2

Found 3 different cloud patterns: Cloudflare Workers with Workers AI (Website feed); Blob Storage (Vercel Blob) (Website feed); Cloudflare R2 Storage (Website feed)

**Remediation**:
1. Review the conflicting patterns across repositories
2. Create an ADR documenting the standard pattern to use
3. Create migration tickets for non-compliant repos
4. Update linting rules to enforce the standard

## ICS-005: Multiple integration patterns in use

**Priority**: P2

Found 5 different integration patterns: OpenAI API for AI Generation (Website feed); SendGrid Email Service (Website feed); Twilio SMS/Voice (Website feed); Replicate AI Models (Website feed); Hugging Face Inference (Website feed)

**Remediation**:
1. Review the conflicting patterns across repositories
2. Create an ADR documenting the standard pattern to use
3. Create migration tickets for non-compliant repos
4. Update linting rules to enforce the standard

## ICS-006: Multiple architecture patterns in use

**Priority**: P2

Found 3 different architecture patterns: Inngest for Background Jobs (Website feed); SM-2 Spaced Repetition Algorithm (Website feed); RAG (Retrieval Augmented Generation) (Website feed)

**Remediation**:
1. Review the conflicting patterns across repositories
2. Create an ADR documenting the standard pattern to use
3. Create migration tickets for non-compliant repos
4. Update linting rules to enforce the standard
